# @package _global_

defaults:
  - /data: bsccm_tiny
  - /split: stratified
  - /model: baseline_unet
  - /trainer: default
  - /logging: default
  - _self_

run:
  run_name: baseline_unet
  tags:
    - baseline
    - unet
