# Optional: override default data root used by your config.
# BSCCM_I2I_DATA_DIR=./data

# Required only when upstream Dryad download needs authentication.
# Used by bsccm.download_dataset(token=...).
BSCCM_DRYAD_TOKEN=your_token_here
